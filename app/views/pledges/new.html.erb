<%= form_for ([@project, @pledge]) do |f| %>

<%= f.label :amount %><br>
<%= f.number_field :amount %>

<%= f.submit "Pledge Now" %>

<%= render 'layouts/shared/form_errors', :target => f.object %>

<%end%>