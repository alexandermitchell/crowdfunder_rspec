<% unless target.errors.empty? %>
    <div class="alert alert-error">
      <strong>Try again after fixing the following things:</strong>
      <ul>
        <% target.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <%end%>
      </ul>
    </div>
<%end%>