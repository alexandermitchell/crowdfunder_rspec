<div class="jumbotron">
  <div class="container">
      <h1><%= link_to target.title, project_path(target) %></h1>
      <p><%= target.teaser %></p>
      <p><%= link_to "Learn More", project_path(target), class: 'btn btn-primary' %></p>
  </div>
</div>

<br />